apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: dex
  namespace: dex
spec:
  gateways:
    - istio-system/platform-gateway
  hosts:
    - 'auth.bychkow.ski'
  http:
    - route:
        - destination:
            host: dex.dex.svc.cluster.local
            port:
              number: 5555
